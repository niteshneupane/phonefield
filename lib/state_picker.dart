import 'package:flutter/material.dart';

///
class StatePicker extends StatelessWidget {
  ///
  const StatePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("State Picker");
  }
}
